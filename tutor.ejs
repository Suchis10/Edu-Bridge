<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Find a Tutor</title>
  <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gradient-to-br from-slate-900 via-blue-900 to-indigo-900 min-h-screen py-10 px-6 text-white font-sans">

  <h1 class="text-4xl font-extrabold text-center mb-12 tracking-tight text-white drop-shadow-md">📍 Get Teacher Nearby You</h1>

  <div class="grid grid-cols-1 sm:grid-cols-2 md:grid-cols-3 gap-8 max-w-7xl mx-auto">

    <% teachers.forEach(tutor => { %>
      <div class="bg-white/10 backdrop-blur-lg shadow-xl rounded-2xl p-6 text-center hover:scale-105 transition border border-white/20">
        <h2 class="text-xl font-bold <%= tutor.colorClass %> mb-2"><%= tutor.name %></h2>
        <p class="text-gray-200 mb-2"><%= tutor.description %></p>
        <p><span class="font-semibold text-white">Language:</span> <%= tutor.language %></p>
        <p><span class="font-semibold text-white">Experience:</span> <%= tutor.experience %> years</p>
        <p><span class="font-semibold text-white">Rate:</span> ₹<%= tutor.rate %>/hr</p>
        <button class="mt-4 <%= tutor.buttonClass %> text-black font-semibold px-4 py-2 rounded-lg">Contact</button>
      </div>
    <% }) %>

  </div>

</body>
</html>
